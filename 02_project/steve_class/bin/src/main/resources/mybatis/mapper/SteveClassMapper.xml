<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.steveleejava.offclass.mapper.SteveClassMapper">

	<select id="searchCardList" parameterType="map" resultType="map">
		SELECT * 
		FROM class_offline
	</select>

</mapper>
